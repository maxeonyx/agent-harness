Because it's common to use a web application for a desktop application or such, for example, Outlook, new, for example, Microsoft Teams, and obviously a web browser itself, let's build in an MCP server into the harness itself that can consume a remote debugging endpoint, so that you don't have to have a separate piece of software running just to control a Chromium-based web browser app.