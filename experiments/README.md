# Experiments

Disposable spikes live here.

Rules:

- Each spike gets its own subdirectory.
- Each spike must name the requirement it is testing.
- Each spike must have tests or explicit review criteria before implementation.
- Each spike must produce an outcome document under `docs/process/` before any
  behavior is considered for core integration.
- Core code must not depend on experiment internals.
